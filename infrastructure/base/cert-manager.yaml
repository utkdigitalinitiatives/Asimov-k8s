---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: jetstack
  namespace: solr-system
spec:
  interval: 24h
  url: https://charts.jetstack.io
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: solr-system
spec:
  interval: 30m
  chart:
    spec:
      chart: cert-manager
      version: 'v1.16.x'
      sourceRef:
        kind: HelmRepository
        name: jetstack
        namespace: solr-system
      interval: 12h
  # Wait for cert-manager pods to be ready before considering deployment complete
  install:
    crds: Create
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
  # Health checks to ensure cert-manager is fully operational
  test:
    enable: false
  values:
    # Install CRDs with the cert-manager Helm chart
    installCRDs: true

    # Global settings
    global:
      leaderElection:
        namespace: solr-system

    # Pod settings for AKS user nodes
    nodeSelector:
      kubernetes.azure.com/mode: user

    resources:
      requests:
        cpu: 10m
        memory: 32Mi
      limits:
        cpu: 100m
        memory: 128Mi

    # Webhook configuration
    webhook:
      nodeSelector:
        kubernetes.azure.com/mode: user
      resources:
        requests:
          cpu: 10m
          memory: 32Mi
        limits:
          cpu: 100m
          memory: 128Mi

    # CA injector configuration
    cainjector:
      nodeSelector:
        kubernetes.azure.com/mode: user
      resources:
        requests:
          cpu: 10m
          memory: 32Mi
        limits:
          cpu: 100m
          memory: 128Mi
