---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: prometheus-community
  namespace: monitoring
spec:
  interval: 24h
  url: https://prometheus-community.github.io/helm-charts
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
  namespace: monitoring
spec:
  interval: 30m
  chart:
    spec:
      chart: kube-prometheus-stack
      version: '>=87.0.0 <88.0.0'
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
        namespace: monitoring
      interval: 12h
  # Install CRDs with the chart
  install:
    crds: Create
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
  values:
    # Manage CRDs via the chart's crds subchart. On a fresh install (old unmanaged CRDs
    # deleted during teardown) Helm installs the v0.92.0 CRDs from the subchart's crds/ dir.
    crds:
      enabled: true

    # AKS runs the control plane as a managed service and does not expose the
    # kube-controller-manager, kube-scheduler, or kube-proxy metrics endpoints. Disabling
    # these components drops their unscrapeable ServiceMonitors AND the permanently-firing
    # false-positive KubeControllerManagerDown / KubeSchedulerDown / KubeProxyDown critical
    # alerts (plus the *InstanceUnreachable siblings) — the rule groups are gated on each
    # component's `enabled` flag.
    kubeControllerManager:
      enabled: false
    kubeScheduler:
      enabled: false
    kubeProxy:
      enabled: false

    # Prometheus configuration
    prometheus:
      prometheusSpec:
        # Retention settings
        retention: 30d
        retentionSize: "45GB"

        # Storage configuration for Azure Managed Disks
        storageSpec:
          volumeClaimTemplate:
            spec:
              storageClassName: managed-csi
              accessModes: ["ReadWriteOnce"]
              resources:
                requests:
                  storage: 50Gi

        # Resource limits
        resources:
          requests:
            cpu: 500m
            memory: 2Gi
          limits:
            cpu: 2000m
            memory: 4Gi

        # Schedule on user nodes
        nodeSelector:
          kubernetes.azure.com/mode: user

        # Enable ServiceMonitor discovery across all namespaces
        serviceMonitorSelectorNilUsesHelmValues: false
        podMonitorSelectorNilUsesHelmValues: false
        ruleSelectorNilUsesHelmValues: false

        # Enable administrative APIs
        enableAdminAPI: true

    # Grafana configuration
    grafana:
      enabled: true

      # Admin credentials from Azure Key Vault via External Secrets (grafana-admin secret,
      # synced by grafana-admin-externalsecret.yaml). Username fixed to "admin".
      admin:
        existingSecret: grafana-admin
        userKey: admin-user
        passwordKey: admin-password

      # Persistent storage for dashboards
      persistence:
        enabled: true
        storageClassName: managed-csi
        accessModes: ["ReadWriteOnce"]
        size: 10Gi

      # Resource limits
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          cpu: 500m
          memory: 512Mi

      # Schedule on user nodes
      nodeSelector:
        kubernetes.azure.com/mode: user

      # Default dashboards
      defaultDashboardsEnabled: true

      # Public access via Traefik + Let's Encrypt (cert-manager).
      # DNS: grafana.lib.utk.edu -> 68.220.244.21 (Asimov Traefik LB, IngressClass "traefik").
      ingress:
        enabled: true
        ingressClassName: traefik
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-prod
          traefik.ingress.kubernetes.io/router.entrypoints: websecure
        hosts:
          - grafana.lib.utk.edu
        path: /
        pathType: Prefix
        tls:
          - secretName: grafana-tls
            hosts:
              - grafana.lib.utk.edu

      # Grafana configuration
      grafana.ini:
        server:
          domain: grafana.lib.utk.edu
          root_url: "https://grafana.lib.utk.edu"
          serve_from_sub_path: false
        analytics:
          check_for_updates: true

    # Alertmanager configuration
    alertmanager:
      alertmanagerSpec:
        # Storage for Alertmanager
        storage:
          volumeClaimTemplate:
            spec:
              storageClassName: managed-csi
              accessModes: ["ReadWriteOnce"]
              resources:
                requests:
                  storage: 5Gi

        # Resource limits
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi

        # Schedule on user nodes
        nodeSelector:
          kubernetes.azure.com/mode: user

    # Prometheus Operator configuration
    prometheusOperator:
      resources:
        requests:
          cpu: 100m
          memory: 128Mi
        limits:
          cpu: 500m
          memory: 256Mi

      nodeSelector:
        kubernetes.azure.com/mode: user

    # Node exporter - runs on all nodes as DaemonSet
    nodeExporter:
      enabled: true

    # Kube-state-metrics configuration
    kube-state-metrics:
      resources:
        requests:
          cpu: 10m
          memory: 64Mi
        limits:
          cpu: 100m
          memory: 128Mi

      nodeSelector:
        kubernetes.azure.com/mode: user

    # Default service monitors (enabled by default)
    defaultRules:
      create: true
      rules:
        alertmanager: true
        etcd: true
        configReloaders: true
        general: true
        k8s: true
        kubeApiserverAvailability: true
        kubeApiserverBurnrate: true
        kubeApiserverHistogram: true
        kubeApiserverSlos: true
        kubeControllerManager: true
        kubelet: true
        kubeProxy: true
        kubePrometheusGeneral: true
        kubePrometheusNodeRecording: true
        kubernetesApps: true
        kubernetesResources: true
        kubernetesStorage: true
        kubernetesSystem: true
        kubeSchedulerAlerting: true
        kubeSchedulerRecording: true
        kubeStateMetrics: true
        network: true
        node: true
        nodeExporterAlerting: true
        nodeExporterRecording: true
        prometheus: true
        prometheusOperator: true
