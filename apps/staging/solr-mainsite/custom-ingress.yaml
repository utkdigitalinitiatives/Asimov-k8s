---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: solr-staging-custom
  namespace: solr-mainsite-staging
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - staging.libsolr.lib.utk.edu
    secretName: solr-staging-tls
  rules:
  - host: staging.libsolr.lib.utk.edu
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: solr-mainsite-staging-solrcloud-common
            port:
              number: 8983
