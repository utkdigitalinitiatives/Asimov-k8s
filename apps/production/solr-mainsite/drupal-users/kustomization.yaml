apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: solr-mainsite

resources:
  - cronjob.yaml
  # One file per Drupal site. Uncomment a site only after its infra repo has
  # applied the Key Vault passwords and the ESO grant (see the file header).
  # - site-lib-main.yaml
  # - site-mccarthy.yaml

configMapGenerator:
  - name: solr-drupal-users-script
    files:
      - reconcile-drupal-users.py
